<% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- <form action="/users" method="post" accept-charset="UTF-8">
<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <label for="username">username:</label>
  <input type="text" id="username" name="user[username]"><br>
  <label for="email">email:</label>
  <input type="text" id="email" name="user[email]"><br>
  <label for="password">password:</label>
  <input type="text" id="password" name="user[password]"><br>
  <input type="submit" value="Submit">
</form> -->

<!-- <%= form_tag("/users", method: "post") do%>
  <%= label_tag('username', 'Username') %>
  <%= text_field_tag('username', name="user[username]") %>
  <%= label_tag('email', 'Email') %>
  <%= text_field_tag('email', name="user[email]") %>
  <%= label_tag('password', 'Password') %>
  <%= text_field_tag('password', name="user[password]") %>
  <%= submit_tag('submit') %>
<% end %> -->

<%= form_with model: @user do |form| %>
    <%= form.label :username, "Username"%> 
    <%= form.text_field :username, placeholder: "User_name2021"%> <br><br>
    <%= form.label :email, "Email"%> 
    <%= form.text_field :email, placeholder: "email@domain.com"%> <br><br>
    <%= form.label :password, "Password"%> 
    <%= form.password_field :password %> 
<br><br>
    <%= submit_tag("Submit") %>
  <% end %>